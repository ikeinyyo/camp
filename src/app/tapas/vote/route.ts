import { NextRequest, NextResponse } from "next/server";
import { getRequestUrl } from "@/lib/admin-auth";
import { readUserSessionToken, USER_COOKIE_NAME } from "@/lib/user-session";
import { getUserById } from "@/lib/users";
import { OwnTapaVoteError, submitTapaVote } from "@/lib/tapas";
export async function POST(request: NextRequest) { const session = readUserSessionToken(request.cookies.get(USER_COOKIE_NAME)?.value); if (!session) return NextResponse.redirect(getRequestUrl(request, "/login"), 303); const data = await request.formData(); const user = await getUserById(session.activeUserId); if (!user) return NextResponse.redirect(getRequestUrl(request, "/login"), 303); try { await submitTapaVote({ userId: user.id, displayName: user.displayName, firstId: String(data.get("firstId")), secondId: String(data.get("secondId")), thirdId: String(data.get("thirdId")) }); return NextResponse.redirect(getRequestUrl(request, "/tapas?voted=true"), 303); } catch (error) { const duplicate = typeof error === "object" && error !== null && "statusCode" in error && error.statusCode === 409; const own = error instanceof OwnTapaVoteError; return NextResponse.redirect(getRequestUrl(request, `/tapas?error=${duplicate ? "duplicate" : own ? "own" : "vote"}`), 303); } }
